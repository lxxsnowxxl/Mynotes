<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.RECORD_AUDIO" />

    <uses-permission
        android:name="android.permission.VIBRATE" />

    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Permite silenciar temporalmente el canal de sonidos de sistema mientras
        el teclado está visible. Es un permiso normal: no muestra diálogo al
        usuario y el canal se restaura al cerrar el IME o salir de MyNotes.
    -->
    <uses-permission
        android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
        Necesario únicamente para entregar al instalador del sistema los APK
        descargados desde GitHub Releases. En Android 8+ el usuario debe
        autorizar explícitamente a MyNotes en "Instalar apps desconocidas".
    -->
    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
        La aplicación funciona sin micrófono. Declararlo como opcional evita
        que Google Play excluya tablets, Chromebooks u otros equipos que no
        dispongan de uno. La grabación de voz se desactiva en tiempo de
        ejecución cuando el hardware no existe.
    -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyNotes">

        <activity
            android:name=".ui.AttachmentViewerActivity"
            android:exported="false"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:theme="@style/Theme.MyNotes" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:theme="@style/Theme.MyNotes.Starting"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

            <!--
                Permite crear una nota directamente desde Compartir en
                navegadores, YouTube, Spotify y otras apps que envían texto.
            -->
            <intent-filter>

                <action
                    android:name="android.intent.action.SEND" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <data
                    android:mimeType="text/plain" />

            </intent-filter>

        </activity>

        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.QuickNoteWidgetProvider"
            android:exported="true"
            android:label="@string/widget_new_note">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_note_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.RecentNotesWidgetProvider"
            android:exported="true"
            android:label="@string/widget_recent_notes">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recent_notes_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.FavoritesWidgetProvider"
            android:exported="true"
            android:label="@string/widget_favorites">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/favorites_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.FocusNoteWidgetProvider"
            android:exported="true"
            android:label="@string/widget_focus_note">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/focus_note_widget_info" />
        </receiver>
<receiver
            android:name=".widget.WidgetActionReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />

        </provider>

    </application>

</manifest>
